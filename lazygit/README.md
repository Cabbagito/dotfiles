# Lazygit Configuration

Minimal configuration for lazygit terminal UI.

## Settings

| Setting | Value | Description |
|---------|-------|-------------|
| `notARepository` | `skip` | Silently skip directories that aren't git repos |

This prevents lazygit from showing error prompts when launched outside a repository.

## Shell Alias

```bash
lg  # → lazygit
```
